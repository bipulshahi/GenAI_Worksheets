## **DATA PREPROCESSING IN MACHINE LEARNING**

### *Handling Missing Values • Feature Scaling • Data Encoding*

**Student Name:** _______________________________
**Roll No:** ___________________
**Course / Semester:** __________________________
**Date:** _____________________

--

## **THE SCENARIO (WHY PREPROCESSING MATTERS)**

### **Real-World Scenario**

You are the **Chief Data Scientist** at **MediPredict AI**, a company deploying a machine-learning system to classify **patient risk levels** in emergency hospitals.

The ML model architecture is strong —
but predictions are **unreliable and unsafe**.

After investigation, you discover the real issue:

>  *The data is messy, incomplete, and unprocessed.*

Your responsibility is to **prepare the data correctly before any model is trained**.

--

### **Raw Patient Data (UNLABELLED & UNPROCESSED)**

> *This dataset has **NO risk labels**.
> Your task is NOT to predict — only to identify preprocessing needs.*

| Patient ID | Age  | Income (₹) | City    | Blood Pressure |
| ---------- | ---- | ---------- | ------- | -------------- |
| P101       | 25   | 25000      | Delhi   | 120            |
| P102       | NULL | 800000     | Mumbai  | 140            |
| P103       | 60   | 30000      | Chennai | NULL           |
| P104       | 35   | 45000      | Delhi   | 130            |
| P105       | 40   | NULL       | Mumbai  | 125            |

---

### **Identify Data Problems (Tick all that apply)**

* [ ] Missing Age values
* [ ] Missing Income values
* [ ] Missing Blood Pressure values
* [ ] Large variation in numerical ranges
* [ ] Categorical (text) data present

---

### **Reflection**

Why can’t this raw data be directly used to train a machine-learning model?

* ML models cannot handle missing values directly
* Different feature scales bias learning
* Categorical text data must be converted to numbers
* Raw data leads to unreliable predictions
* **Raw data contains missing values, inconsistent scales, and categorical features, which ML models cannot process directly without preprocessing.**

## **ML TRAINING DATA (LABELLED BUT DIRTY)**

### **Historical Training Data**

> This dataset **contains risk labels**,
> but still has **missing values** and **unprocessed features**.

| Age  | Income (₹) | City    | Risk Level |
| ---- | ---------- | ------- | ---------- |
| 25   | 25000      | Delhi   | Low        |
| NULL | 40000      | Delhi   | Low        |
| 55   | 30000      | Chennai | High       |
| 45   | NULL       | Mumbai  | High       |
| 35   | 45000      | Delhi   | Low        |

---

### **Exercise 1 — Handling Missing Values**

Choose the **most appropriate strategy**:

| Column     | Strategy                     |
| ---------- | ---------------------------- |
| Age        | [ ] Mean [ ] Median [ ] Drop |
| Income     | [ ] Mean [ ] Median [ ] Drop |
| City       | [ ] Mode [ ] Drop            |
| Risk Level | [ ] Drop row [ ] Keep        |

Explain **one choice** you made:

| Column     | Best Strategy | Reason             |
| ---------- | ------------- | ------------------ |
| Age        | Median      | Robust to outliers |
| Income     | Median      | Income is skewed   |
| City       | Mode        | Categorical        |
| Risk Level | Keep        | Target variable    |
**Dropping rows is acceptable only if data is large (discussion point).**

---

### **Exercise 2 — Feature Scaling**

Which columns require scaling?

* [ ] Age
* [ ] Income
* [ ] City
* [ ] Risk Level

Why is feature scaling necessary?

* Age and Income have different units and ranges
* Scaling prevents feature dominance

---

### **Exercise 3 — Data Encoding**

Which feature must be encoded before training?

* [ ] Age
* [ ] Income
* [ ] City
* [ ] Risk Level

ML models require numerical input
* City is categorical text data

---

## **HANDS-ON: PANDAS DATAFRAME & MISSING VALUES**

### **Hands-On Exercise 1: Create a DataFrame**

```python
import pandas as pd

data = {
    "Age": [25, None, 55, 45, 35],
    "Income": [25000, 40000, 30000, None, 45000],
    "City": ["Delhi", "Delhi", "Chennai", "Mumbai", "Delhi"],
    "Risk_Level": ["Low", "Low", "High", "High", "Low"]
}

df = pd.DataFrame(data)
df
```

---

### **Hands-On Exercise 2: Detect Missing Values**

```python
df.isnull()
```

```python
df.isnull().sum()
```

**Write the number of missing values in each column:**

---

---

### **Hands-On Exercise 3: Handle Missing Values**

```python
df["Age"].fillna(df["Age"].median(), inplace=True)
df["Income"].fillna(df["Income"].median(), inplace=True)
```

Why is **median** often preferred over mean?

---

---

---

## **FEATURE SCALING, ENCODING & NEW DATA**

### **Feature Scaling**

```python
from sklearn.preprocessing import StandardScaler

scaler = StandardScaler()
df[["Age", "Income"]] = scaler.fit_transform(df[["Age", "Income"]])
df
```

Which problem does scaling solve?

* [ ] Feature dominance
* [ ] Missing values
* [ ] Categorical data

---

### **Encoding Categorical Variables**

```python
df_encoded = pd.get_dummies(df, columns=["City"], drop_first=True)
df_encoded
```

Why can’t ML models work directly with text data?

---

---

### **Apply to New Unknown Data (NO LABEL)**

| Age | Income | City    |
| --- | ------ | ------- |
| 38  | 50000  | Chennai |

```python
new_data = pd.DataFrame({
    "Age": [38],
    "Income": [50000],
    "City": ["Chennai"]
})
```

Why must we use the **same scaler and encoding** learned earlier?

---

---

## **THE BIG PICTURE — AI / ML / DL HIERARCHY**

| Level                   | Description                           |
| ----------------------- | ------------------------------------- |
| Artificial Intelligence | Machines mimicking human intelligence |
| Machine Learning        | Learning patterns from data           |
| Deep Learning           | Neural-network-based ML               |
| **Data Preprocessing**  | **Foundation for all ML systems**     |

---

## **FINAL SELF-CHECK**

* [ ] I understand labelled vs unlabelled data
* [ ] I can handle missing values using Pandas
* [ ] I can scale numerical features
* [ ] I can encode categorical data
* [ ] I understand why preprocessing is critical

---

## **Final Takeaway**

> **“Machine learning models don’t fail first — data does.”**

---
